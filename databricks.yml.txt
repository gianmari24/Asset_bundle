# This is a Databricks asset bundle definition for eds_driver.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
# See https://learn.microsoft.com/it-it/azure/databricks/dev-tools/bundles/variables for commonly used variables.
bundle:
  # Replace with your bundle name
  name: bundle_template

include:
  - resources/*.yml

variables:
  v_root_path:
    default: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    description: 'Default root path for technical files  (currently used only in dev)'
  v_file_path: 
    default: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}/files
    description: 'Default path to deploy (currently used only in dev)'
  v_mode: 
    default: development

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    variables:
      v_mode: development
      # v_root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.target}/${bundle.name}
      # v_file_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.target}/${bundle.name}/files
    mode: ${var.v_mode}
    default: true
    # presets:
    #   source_linked_deployment: true
    workspace:
      # needs to be commented out if using in the workspace UI
      # host: https://adb-3425487887631424.4.azuredatabricks.net
      root_path: ${var.v_root_path}
      file_path: ${var.v_file_path}
    permissions:
    - user_name: ${workspace.current_user.userName}
      level: CAN_MANAGE
    # - group_name: 
      # level: CAN_MANAGE


  cert:
    variables:
      v_mode: production
      v_root_path: /Workspace/3NF/ods_elt/.bundle/${bundle.name} # If not explicitly needed, leave the root path like this
      v_file_path: /Workspace/PATH_TO_CERT # <<<-- UPDATE WITH YOUR PATH HERE --
    mode: ${var.v_mode}
    workspace:
      host: https://adb-3425487887631424.4.azuredatabricks.net
      root_path: ${var.v_root_path}
      file_path: ${var.v_file_path}
    permissions:
      - user_name: ${workspace.current_user.userName}
        level: CAN_MANAGE
      - group_name: EDS_DEVELOPER_3NF_PROD
        level: CAN_MANAGE         
    run_as:
      service_principal_name: 219a0b9f-78f8-45ed-a10d-4e030ffcd6e4


  prod:
    variables:
      v_mode: production
      v_root_path: /Workspace/3NF/ods_elt/.bundle/${bundle.name} # If not explicitly needed, leave the root path like this
      v_file_path: /Workspace/PATH_TO_PROD # <<<-- UPDATE WITH YOUR PATH HERE -- 
    mode: ${var.v_mode}
    workspace: 
      host: https://adb-6849659081642841.1.azuredatabricks.net
      root_path: ${var.v_root_path}
      file_path: ${var.v_file_path}
    permissions:
      - user_name: ${workspace.current_user.userName}
        level: CAN_MANAGE
      - group_name: EDS_DEVELOPER_3NF_PROD
        level: CAN_MANAGE         
    run_as:
      service_principal_name: 219a0b9f-78f8-45ed-a10d-4e030ffcd6e4


sync:
  # include:
  #   - notebooks/*
  exclude: 
    - .gitignore
    - ./*.yml
    - README.md
    - schema.json