# This is a Databricks asset bundle definition.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
# See https://learn.microsoft.com/it-it/azure/databricks/dev-tools/bundles/variables for commonly used variables.
bundle:
  # Replace with your bundle name
  name: bundle_bakehouse

include:
  - resources/*.yml
  - variables/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    variables:
      v_mode: development
      # v_root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.target}/${bundle.name}
      # v_file_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.target}/${bundle.name}/files
    mode: ${var.v_mode}
    default: true
    # presets:
    #   source_linked_deployment: true
    workspace:
      # needs to be commented out if using in the workspace UI
      host: https://dbc-afeefbf3-0c30.cloud.databricks.com/?o=7474657116973183
      root_path: ${var.v_root_path}
      file_path: ${var.v_file_path}
    permissions:
    - user_name: ${workspace.current_user.userName}
      level: CAN_MANAGE
    # - group_name: 
      # level: group_dab


  qas:
    variables:
      v_mode: production
    mode: ${var.v_mode}
    workspace:
      host: https://dbc-afeefbf3-0c30.cloud.databricks.com/?o=7474657116973183
      root_path: ${var.v_root_path}
      file_path: ${var.v_file_path}
    permissions:
      - user_name: ${workspace.current_user.userName}
        level: CAN_MANAGE
      - group_name: group_dab
        level: CAN_MANAGE         
    run_as:
      service_principal_name: service_principal_user


  prod:
    variables:
      v_mode: production
    mode: ${var.v_mode}
    workspace: 
      host: https://dbc-afeefbf3-0c30.cloud.databricks.com/?o=7474657116973183
      root_path: ${var.v_root_path}
      file_path: ${var.v_file_path}
    permissions:
      - user_name: ${workspace.current_user.userName}
        level: CAN_MANAGE
      - group_name: group_dab
        level: CAN_MANAGE         
    run_as:
      service_principal_name: service_principal_user


sync:
  # include:
  #   - notebooks/*
  exclude: 
    #- .gitignore
    #- ./*.yml
    - README.md
    #- schema.json
